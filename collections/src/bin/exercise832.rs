fn main() {
    // GOAL: Convert a standard String into Pig Latin
    /*
    Rule 1: If a word starts with a consonant, move the first letter to
            the end and add "ay" (first -> irst-fay)

    Rule 2: If a word starts with a vowel, leave it in place and add "hay"
            to the end (apple -> apple-hay)
    */

    let string = String::from("Hello first apple");
}

fn convert_to_piglatin(original: String) -> String {
    // Extract the first letter
    // tip: whitespace separator!!

    // Rule 1 (consonant)

    // Rule 2 (vowel)
}
